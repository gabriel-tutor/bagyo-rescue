DB_USER="postgres"
DB_PASSWORD="postgres"
DB_PORT="5432"
TEST_DB_PORT="5433"

PGADMIN_PORT="5050"
PGADMIN_EMAIL="admin@example.com"
PGADMIN_PASSWORD="admin"

DATABASE_URL="postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_USER}"
TEST_URL="postgresql://${DB_USER}:${DB_PASSWORD}@localhost:${TEST_DB_PORT}/${DB_USER}"
